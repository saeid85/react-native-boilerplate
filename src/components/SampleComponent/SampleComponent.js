import React, { Component } from "react";
import { Text, View } from "react-native";
import styles from "./styles";

export default class RecipeDetails extends Component {
  componentDidMount() {
    // componentDidMount
  }

  render() {
    return (
      <View>
        <Text style={styles.sampleText}>Sample Component</Text>
      </View>
    );
  }
}
